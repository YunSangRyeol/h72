<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="searchMapper">

	<!-- <resultMap id="memberResultMap" type="Member">
		<id property="userid" column="user_id" />
		<result property="userpass" column="user_pass" />
		<result property="name" column="name" />
		<result property="birthdate" column="birthdate" />
		<result property="email" column="email" />
		<result property="phone" column="phone" />
		<result property="postnum" column="postNum" />
		<result property="adrress" column="address" />
		<result property="adressdetail" column="address_detail" />

	</resultMap> -->
<!-- 
	<select id="getCateBag" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_BAG
	</select>

	<select id="getCateProtect" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_PROTECT
	</select>

	<select id="getCateFood" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_FOOD
	</select>

	<select id="getCateTool" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_TOOL
	</select>

	<select id="getCateEtc" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_ETC
	</select>

	<select id="getSearchKeyword" parameterType="search" resultType="search">
		SELECT ITEM_NAME, MIN_PRICE, MIN_SAIL_PRICE, MAIN_IMG FROM
		H72.ITEM_DETAIL_BAG, H72.ITEM_DETAIL_PROTECT, H72.ITEM_DETAIL_FOOD,
		H72.ITEM_DETAIL_TOOL, H72.ITEM_DETAIL_ETC WHERE ITEM_NAME = #{keyword}
	</select>

 -->
</mapper>
